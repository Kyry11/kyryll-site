node_modules/
dist/
.DS_Store

# Vite's dependency pre-bundling cache
.vite/

# These hold the Communication Services connection string. Never commit them.
.dev.vars
.env
.env.*
!.env.example

# wrangler's local state, and the scratch config the deploy writes after
# substituting the storage endpoint and KV namespace id into wrangler.toml.
.wrangler/
worker/wrangler.deploy.toml
