node_modules/
dist/
.DS_Store

# Vite's dependency pre-bundling cache
.vite/

# These hold the Communication Services connection string. Never commit them.
.dev.vars
.env
.env.*
!.env.example

# wrangler's local state, and the scratch configs written by `npm run dev` and
# by the deploy after substituting the KV namespace id into wrangler.toml.
.wrangler/
worker/wrangler.deploy.toml
worker/wrangler.dev.toml
